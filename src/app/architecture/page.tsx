import { PageHero } from "@/components/marketing";
import { ArchitectureDiagramBlock, FeatureCard } from "@/components/ui";
import { architectureBlocks, architectureFlow } from "@/lib/mock-data/architecture";

export default function Architecture(){
  return <main className="marketing"><PageHero tag="Reference architecture" title="Scalable by design. Explainable by default." body="FinTech-AI 2026 separates tenant data, domain services, AI orchestration, and integration adapters so the platform can evolve without compromising security or maintainability."/><section className="content-light"><div className="container"><div className="section-head"><h2>A controlled path from request to auditable action.</h2><p>Every request passes through tenant identity, domain policy, reliable AI controls, isolated data, and observable infrastructure.</p></div><ArchitectureDiagramBlock items={architectureFlow}/><div className="architecture-grid">{architectureBlocks.map((block,index)=><FeatureCard key={block.title} eyebrow={`CAPABILITY ${String(index+1).padStart(2,"0")}`} {...block} body={block.detail}/>)}</div><div className="architecture-principles"><div><span>Predictable AI outputs</span><h3>Schema-constrained generation, retrieval boundaries, confidence thresholds, and human approval.</h3></div><div><span>Operational resilience</span><h3>Idempotent APIs, asynchronous document queues, retries, monitoring, and versioned behavior.</h3></div><div><span>US & EU readiness</span><h3>Regional deployment options, configurable retention, audit evidence, encryption, and role controls.</h3></div></div></div></section></main>;
}
